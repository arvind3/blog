{
  "name": "ab-blog",
  "private": true,
  "scripts": {
    "dev": "hugo serve -D",
    "prebuild": "node -e \"require('fs').rmSync('public',{ recursive: true, force: true })\"",
    "build": "hugo --minify --gc"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.0"
  }
}
